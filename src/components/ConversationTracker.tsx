import * as React from 'react';

export interface ConversationTrackerProps {
    id: number
}

export const ConversationTracker: React.FC<ConversationTrackerProps> =  ({ id: number }) => {

    return (
      <div>
        
      </div>
    );
}